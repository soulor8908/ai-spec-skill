# app/main.py —— FastAPI 入口
# 由 create-ai-spec-app 生成（backend: fastapi）
# 后续 spec:init 创建第一个业务域时由 impl-writer 角色驱动填充。

from fastapi import FastAPI

app = FastAPI(title="{{project-name}}")


@app.get("/health")
async def health():
    return {"status": "ok"}
