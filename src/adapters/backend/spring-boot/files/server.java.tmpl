// apps/api/src/main/java/com/example/AiSpecApplication.java
// 由 create-ai-spec-app 生成（backend: spring-boot）
// 后续 spec:init 创建第一个业务域时由 impl-writer 角色驱动填充。

package com.example;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class AiSpecApplication {
    public static void main(String[] args) {
        SpringApplication.run(AiSpecApplication.class, args);
    }
}
